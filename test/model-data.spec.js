import {suma} from '../src/js/model-data';

test('sumar 1 + 2 es igual a 3', () => {
  expect(suma(1, 2)).toBe(3);
});


describe("validador de contraseñas", ()=>{
  it("debe verificar que la contraseña sea mas de 6 caracteres")
});